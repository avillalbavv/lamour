'use client';import Link from 'next/link';import {ArrowRight} from 'lucide-react';import {Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from '@/components/ui/select';import {Skeleton} from '@/components/ui/skeleton';import {Empty,EmptyHeader,EmptyTitle,EmptyDescription,EmptyContent} from '@/components/ui/empty';import {useStore} from './store-provider';
export function Choice({label,value,onChange,options}:{label:string;value:string;onChange:(v:string)=>void;options:{value:string;label:string}[]}){return <Select value={value} onValueChange={onChange}><SelectTrigger className="select-control" aria-label={label}><SelectValue placeholder={label}/></SelectTrigger><SelectContent>{options.map(o=><SelectItem key={o.value} value={o.value}>{o.label}</SelectItem>)}</SelectContent></Select>}
export function EmptyState({title,text,href='/tienda',cta='Descubrir la tienda'}:{title:string;text:string;href?:string;cta?:string}){return <Empty className="empty-state"><EmptyHeader><EmptyTitle><h3>{title}</h3></EmptyTitle><EmptyDescription>{text}</EmptyDescription></EmptyHeader><EmptyContent><Link className="button" href={href}>{cta}<ArrowRight size={17}/></Link></EmptyContent></Empty>}
export function LoadState(){const {loading,error,reload}=useStore();if(error)return <div className="container empty-state" role="alert"><h3>No pudimos cargar la tienda</h3><p>{error}</p><button className="button" onClick={reload}>Intentar de nuevo</button></div>;if(loading)return <div className="container section loading-grid" aria-label="Cargando productos">{[0,1,2,3].map(i=><Skeleton className="loading-tile" key={i}/>)}</div>;return null}
